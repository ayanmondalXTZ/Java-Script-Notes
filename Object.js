let studentInfo = {
    name: "ayan mondal",
    age: 18,
    roll: 16,
};
console.log(studentInfo.age)
console.log(`My Name is ${studentInfo.name} My Age is ${studentInfo.age} and my roll Number is ${studentInfo.roll}`)